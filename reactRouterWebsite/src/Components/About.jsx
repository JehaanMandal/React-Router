import React from 'react'

function About() {
    return (
        <div className='h-100 bg-indigo-200'>
        <h1 className='text-center text-3xl p-3 font-extrabold text-emerald-700 underline my-2'>About</h1> 
            <p className='p-8 text-xl text-blue-700 font-semibold font-mono'>React Router DOM is a library for managing navigation in React applications, enabling seamless client-side routing. It allows developers to create dynamic, single-page applications (SPAs) with routes that change the URL without reloading the page. Using components like BrowserRouter, Routes, and Link, it makes defining and navigating between pages simple. React Router DOM also supports nested routes, URL parameters, and protected routes, providing full control over navigation in a React project.</p>

        </div>
    )
}

export default About
